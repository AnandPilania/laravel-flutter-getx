import 'package:{{APP_NAME}}/app/models/user.dart' as model;

class {{NAME}}Mock extends MockBase {
  static model.User result(data) {
    return model.Result(
      user: model.Result(
        id: 1, 
        name: 'My Name',
        email: 'example@email.com',
      ),
    );
  }
}
